@model BonVoyage_TravelAgency.Models.BookingHotelFilterModel

@{
    ViewData["Title"] = "Bookings Hotels";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    label {
        font-weight: bold;
        font-family: 'Calypso', Arial, sans-serif;
    }
</style>


<h3>Hotel bookings</h3>
<br />

<form method="get">
    <div>
        <label>User: </label>
        <input asp-for="User" />

        <label>Status: </label>
        <input asp-for="Status" />
        <button type="submit" style = "margin-top: 0px;" class="btn btn-default">Filter</button>
       @*  <input type="submit" value="Filter" /> *@
    </div>
</form>

<br />

<div class="table-container text-center">
    <table class="table">
        <thead>
            <tr>
                <th>User</th>
                <th>User email</th>
                <th>Hotel</th>
                <th>Booking date</th>
                <th>Number of people</th>
                <th>Status</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @if (Model.BookingsHotels != null) 
            {
                @foreach (var item in Model.BookingsHotels)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.User)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.UserMail)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Hotel)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.BookingDate)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.NumberOfPeople)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Status)
                        </td>    

                        <td>                            
                           <a asp-controller="BookingHotel" asp-action="Edit" asp-route-id="@item.BookingHotelId" class="link">Edit status |</a>
                            <a href="#" onclick="confirmDelete(@item.BookingHotelId)">Delete</a>
                        </td>
                    </tr>
                }
            } 
        </tbody>
    </table>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <script>
        $(document).ready(function () {
            var errorMessage = '@TempData["ErrorMessage"]';
            if (errorMessage) {
                Swal.fire({
                    icon: 'error',
                    title: 'Error',
                    text: errorMessage
                });
            }
       });

        function confirmDelete(id) {
            Swal.fire({
                title: 'Are you sure?',
                text: "You won't be able to revert this!",
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#3085d6',
                cancelButtonColor: '#d33',
                confirmButtonText: 'Yes, delete it!'
            }).then((result) => {
                if (result.isConfirmed) {
                    $.ajax({
                        url: '@Url.Action("DeleteConfirmed", "BookingHotel")',
                        type: 'POST',
                        data: { id: id },
                        success: function (response) {
                            if (response.success) {
                                Swal.fire(
                                    'Deleted!',
                                    response.message,
                                    'success'
                                ).then(() => {
                                    location.reload();
                                });
                            } else {
                                Swal.fire(
                                    'Error!',
                                    response.message,
                                    'error'
                                );
                            }
                        },
                        error: function (xhr, status, error) {
                            Swal.fire(
                                'Error!',
                                'An error occurred while deleting the Booking.',
                                'error'
                            );
                        }
                    });
                }
            });
        }

    </script>

}
